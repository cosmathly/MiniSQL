#include <bits/stdc++.h>
#include <fcntl.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>
#include "Buffer_Manager.h"
#include <deque>
#include <list>
using namespace std;
typedef enum Data_Type { Int, Char, Float } data_type;
typedef int node_pointer;
int main()
{
    stringstream ss;
    string str;
    int val = -1;
    ss << val;
    ss >> str;
    cout<<str;
    //if(val==1) cout<<"ok"<<endl;
    return 0;
}